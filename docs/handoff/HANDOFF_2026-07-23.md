# Handoff — 2026-07-23

## 완료된 작업 (이번 세션)

### MomentLoop 대규모 개선 (`8a6851d`)

이슈 #41~#44 구현 완료 및 close.

#### 1. 업로드 세션 단위 그룹핑 (#43)
- `setlog_posts.session_id VARCHAR(64)` 컬럼 마이그레이션 추가
- 업로드 시 `DateTime.now().millisecondsSinceEpoch` 기반 session_id 생성 → 같은 업로드의 모든 파일이 하나의 그룹
- 프론트: `_groups` getter (`session_id` 기준 그룹핑 → `List<List<Map>>`)

#### 2. 게시글 상세 + 이모지 반응 (#41)
- `setlog_reactions` 테이블 생성 (`couple_id + session_id + user_id + emoji`, UNIQUE)
- `POST /api/setlog/reaction` 토글 엔드포인트 (같은 이모지 재클릭 = 취소)
- `GET /api/setlog`에 `session_reactions` 서브쿼리 포함
- `_PostDetailPage`: PageView(미디어) + ❤️😍🥰😂😮 이모지 반응 바

#### 3. 하이라이트·스토리 뷰어 재설계 (#42)
- `_HighlightStrip`: 그룹당 버블 1개, 다중 미디어면 숫자 배지
- `_StoryViewer`: PageView(바깥 스와이프 = 그룹 이동) + GestureDetector(안쪽 탭 = 미디어 이동)
- Progress bar = 현재 그룹의 미디어 개수
- seen 추적을 `session_id` 기준으로 변경

#### 4. 업로드 한도 10개 합산 (#44)
- `_maxPhotos=3, _maxVideos=3` → `_maxMedia=10` (합산)

---

## 현재 상태

| 항목 | 상태 |
|------|------|
| 브랜치 | `main` |
| 최신 커밋 | `8a6851d` |
| 릴리즈 | v1.1.0 예정 |
| 베타 이슈 | #20 (와이파인더) — #31만 남음 (ready-for-human) |

---

## 아키텍처 현황

- **Flutter Web** (포트 7357) — `apps/secret_base_app/`
- **Node.js + Express + Socket.IO** (포트 4100) — `services/realtime-server/`
- **MariaDB** — `setlog_posts`, `setlog_reactions` (신규), `map_pins` 등
- **Redis** — Pub/Sub, 게임 세션

### 주요 파일

| 파일 | 역할 |
|------|------|
| `services/realtime-server/src/routes.js` | REST API 전체 |
| `services/realtime-server/src/socket.js` | 소켓 이벤트 |
| `apps/secret_base_app/lib/screens/archive/moment_loop_screen.dart` | MomentLoop 전체 |
| `apps/secret_base_app/lib/screens/map_screen.dart` | 비밀지도 |

---

## 다음 세션 우선순위

1. **#31 베타 준비·실행** (ready-for-human) — 실커플 3~5쌍 비공개 베타
2. MomentLoop UX 피드백 반영 (베타 후)
3. 비밀지도 couple/auth 스코핑 (`/api/map` 현재 미적용 — memory: secret_map_privacy_risk)

---

## 알려진 이슈 / 주의사항

- 기존 포스트(session_id = NULL)는 이모지 반응 버튼이 표시되나 서버 저장 안 됨 (session_id 없으면 `_toggleReaction` early return)
- `services/realtime-server/.env` — 절대 커밋 금지 (DB URL, JWT_SECRET, API 키 포함)
- SSH 키: `~/Downloads/ssh-key-2026-07-06.key`
- Tailscale 서버 IP: `100.97.58.29`

---

## 로컬 실행 방법

```bash
# 백엔드
cd services/realtime-server && npm run dev

# 프론트 (별도 터미널)
cd apps/secret_base_app
flutter run -d chrome --web-hostname localhost --web-port 7357 \
  '--dart-define=SOCKET_URL=http://localhost:4100' \
  '--dart-define=GOOGLE_CLIENT_ID=999734545507-blrukf3rd81kse4j5bjhm93ppsoaniat.apps.googleusercontent.com'
```
