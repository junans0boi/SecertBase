고스톱(2인 맞고) 구현 계획 — 확정안
확정된 결정
항목	결정
승부 단위	단판제, 판마다 재화 이동
판돈	방장이 점당 100/500/1000 선택 (lobby metadata)
정산	최종점수 × 점당 × 배수, 올인 캡
재화	선행 슬라이스로 분리. 시작 잔액 10,000 / 데일리 보너스 500
규칙	기본 점수 + 고배수 + 피박/광박 + 흔들기/폭탄 + 쪽/뻑/따닥/판쓸이 + 고박 + 총통 + 비광 반쪽 광. 멍박·국진선택 제외
승리 기준	7점 이상 시 고/스톱 선언
나가리	자동 재배분 + 배수 이월 (×2, ×4)
카드	코드 기반 CustomPainter, 전통 화투 감성 (홍/흑/금 팔레트, 월별 상징)
타임아웃	없음. 기존 이탈 배너 + restore 패턴. 정산은 정상 종료 시에만
내가 추가로 결정한 것
선(먼저 치는 사람): 첫 판 랜덤, 나가리 재배분 시 선 유지
같은 월 바닥 2장 매칭 시: 어느 장을 가져갈지 플레이어가 선택 (탭)
흔들기: 같은 월 3장 들고 시작하면 선언 여부를 묻는 프롬프트
초보자 지원 (둘 다 규칙을 모르니 핵심 기능):
내 손패 중 낼 수 있는/획득 가능한 카드 하이라이트
획득 영역에 실시간 점수 표시 ("피 6장 → 1장만 더!")
정산 화면에 점수 계산 과정 풀이 ("광 3장 = 3점, 피 12장 = 3점, 2고 = +2점, 피박 ×2 = 16점")
규칙 도움말 시트 (게임 중 ? 버튼)
구현 순서 (수직 슬라이스)
슬라이스 0 — 재화 시스템 (선행, 별도 세션 권장)

DB: wallets(user_id, balance), wallet_transactions(user_id, amount, reason, game_type, created_at)
서버: 잔액 조회 API + 원자적 이체 함수(트랜잭션, 올인 캡 내장) + 데일리 보너스 지급
Flutter: 아케이드 화면 상단 잔액 위젯
슬라이스 1 — 엔진 (gostop-engine.js + node --test)

48장 덱 정의(월/종류/특수속성), 배분(각 10장+바닥 8장), 총통/흔들기 감지
턴 로직: 손패 내기 → 매칭 → 더미 뒤집기 → 매칭 → 획득 확정. 쪽/뻑/따닥/판쓸이 판정
점수 계산 + 박 판정 + 배수 계산 — 가장 테스트가 많이 필요한 부분
고/스톱 상태머신, 나가리 이월
슬라이스 2 — 소켓 (socket.js)

game:gostop:new / play / pick(바닥 선택) / shake / go-stop / state 핸들러, 기존 blackjack 패턴 복제
정산 시 지갑 이체 호출
슬라이스 3 — Flutter (gostop_screen.dart + 카드 위젯)

HwatuCard CustomPainter (별도 파일, 48장 렌더링)
화면: 상대 획득 영역(위) / 바닥+더미(중앙) / 내 손패(아래) / 점수·배수 패널
대기방 판돈 선택 UI, 정산 다이얼로그, 도움말 시트
socket_service.dart 이벤트 연동, arcade_screen.dart 등록
예상 규모: 엔진이 기존 게임 중 최대 (UNO보다 큼). 세션 3~4개 분량.

미해결 질문
재화 시스템을 먼저 만들 때 기존 게임들(윷놀이 등)에도 배팅을 바로 붙일지, 고스톱만 먼저 연동할지
데일리 보너스 지급 시점 — 로그인 시 자동인지, 버튼 눌러 받기인지
이대로 확정하면 HANDOFF.md에 기록해두고, 포트리스 grill로 넘어갈게. 어때?

1. 재화 시스템을 먼저 만들 때 고스톱 -> 윷놀이 -> 우노
2. 데일리 보너스 지급은 접속시 버튼 눌러야만 지급, 접속 해서 버튼 안누르면 지급 X